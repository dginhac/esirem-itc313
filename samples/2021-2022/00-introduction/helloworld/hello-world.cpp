#include <iostream>

// The main function
int main(int argc, char const *argv[]) {
    // Print "hello, world"
    // on the standard output stream (monitor)
    std::cout << "hello, world" << std::endl;
    // End of the program
    return 0;
}
